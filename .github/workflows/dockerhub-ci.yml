name: Build & Push Image
on:
  push:
    branches:
      - main
jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Started pushing Airflow image to Dockerhub"
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Log in dockerhub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: infra/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/airflow:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/airflow:${{ github.sha }}

      # - name: Get Airflow JWT
      #   run: |
      #     TOKEN=$(curl -s -X POST $AIRFLOW_URL/auth/token \
      #       -H "Content-Type: application/json" \
      #       -d "{\"username\":\"admin\",\"password\":\"${{ secrets.AIRFLOW_PASSWORD }}\"}" \
      #       | jq -r .access_token)

      #     echo "TOKEN=$TOKEN" >> $GITHUB_ENV
      #
      # - name: Call Airflow API
      # run: |
      #   curl http://$AIRFLOW_URL/api/v2/dags \
      #     -H "Authorization: Bearer $TOKEN"

      # - name: Run Data Cleaning Pipeline
      #   env:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #   run: |
      #         docker run --rm \
      #         -e AWS_ACCESS_KEY_ID \
      #         -e AWS_SECRET_ACCESS_KEY \
      #         ${{ secrets.DOCKERHUB_USERNAME }}/airflow:latest
